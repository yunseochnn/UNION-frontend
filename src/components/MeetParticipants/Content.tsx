import ProfileList from './ProfileList';

const Content = () => {
  return (
    <div className="mt-2 flex flex-col">
      <ProfileList />
      <ProfileList />
      <ProfileList />
    </div>
  );
};

export default Content;
